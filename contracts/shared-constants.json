{
  "contract": "shared-constants",
  "version": 1,
  "readme": "contracts/README.md",
  "description": "少数几个「两个运行时都要认、而谁也不拥有」的常量。不一致时没有任何报错 —— 只是行为悄悄错开,所以每一条都写清了错开之后用户会看到什么。",
  "notes": [
    "只放**没法单向下发**的常量。能由一侧算出来再传给另一侧的(例如面板圆角经 IPC 下发)不属于这里 —— 那种本来就只有一份。"
  ],
  "constants": [
    {
      "name": "publish_partition_prefix",
      "value": "mosael",
      "why": "发布账号的登录分区名(persist:<前缀>-<accountId>)。两边拼的是**同一个磁盘目录**:后端按它清理/迁移账号目录,Electron 按它创建会话分区。改了其中一个,所有已登录的发布账号的 cookie 会凭空消失,用户得重新登录每一个平台 —— 而且不会有任何报错。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/core/db.py",
          "symbol": "PARTITION_PREFIX"
        },
        {
          "runtime": "electron",
          "location": "electron/publish/accountViews.ts",
          "symbol": "PARTITION_PREFIX"
        }
      ]
    },
    {
      "name": "embed_header_height_px",
      "value": 56,
      "why": "内嵌发布视图从顶部这么多像素处开始铺,上方那条留给「登录中 · 返回」工具栏。Electron 按它定视图边界,前端按它定那条栏的高度。两者不等就会露出一条缝,缝里是 App 自己的顶栏 —— 看着像画面穿帮,而不像一个 bug,所以很容易被当成本来就长这样。原生窗口按钮、应用和素材对比顶栏也共用这一高度,避免中心偏移。",
      "implementations": [
        {
          "runtime": "electron",
          "location": "electron/publish/types.ts",
          "symbol": "EMBED_HEADER_HEIGHT"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/app/App.tsx",
          "symbol": "PUBLISH_BAR_HEIGHT"
        },
        {
          "runtime": "electron",
          "location": "electron/window-chrome.cjs",
          "symbol": "WINDOW_CHROME_HEIGHT"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/lib/windowChrome.ts",
          "symbol": "WINDOW_CHROME_HEIGHT"
        }
      ]
    },
    {
      "name": "note_append_separator",
      "value": "\\n\\n",
      "why": "后端把追加的内容用它接到笔记正文后面;前端**按同一个串判断一次改动是不是纯追加**,是才把它并进正在编辑的草稿(见 features/notes/appendMerge)。两边不等时不会报错 —— 前端只是再也认不出追加,于是每次从逐字稿/字幕导出都把正在写作的人推进冲突条,而那看起来像是「协作就是这么难用」,不像一个可修的 bug。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/notes.py",
          "symbol": "APPEND_SEPARATOR"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/features/notes/appendMerge.ts",
          "symbol": "APPEND_SEPARATOR"
        }
      ]
    },
    {
      "name": "credential_lease_ttl_seconds",
      "value": 30,
      "why": "OAuth 凭据刷新租约的 TTL。后端按它判断「持有者是不是死了」,sidecar 按它算续租间隔(取三分之一)。sidecar 把它猜小了会白白多续几次(无害),猜大了则在真正的 TTL 过去之后才续——租约被别人顶替,于是**两个 sidecar 同时拿同一份一次性 refresh token 去刷新**,先手刚存好的凭据当场失效,用户看到「刚登录就被登出」,偶发且难复现。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/provider_auth.py",
          "symbol": "LEASE_TTL_SECONDS"
        },
        {
          "runtime": "agent-sidecar",
          "location": "agent-sidecar/src/credentials.ts",
          "symbol": "CREDENTIAL_LEASE_TTL_SECONDS"
        }
      ]
    },
    {
      "name": "credential_acquire_timeout_seconds",
      "value": 20,
      "why": "后端 `acquire_lease` 等别人释放的上限,超过才返回 409。**sidecar 必须知道这个数**:它收到一个 409 意味着已经等过这么久,而不是「几秒」。早先 sidecar 的注释写的是「它几秒内会结束」,于是按 400/800/1200ms 退避重试 3 次,以为自己总共等了 2.4 秒 —— 实际这一轮对话在凭据上最多站住约 80 秒。两侧对同一段等待的理解差了三十倍,而没有任何一处同时看得见这两个数。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/provider_auth.py",
          "symbol": "ACQUIRE_TIMEOUT_SECONDS"
        },
        {
          "runtime": "agent-sidecar",
          "location": "agent-sidecar/src/credentials.ts",
          "symbol": "CREDENTIAL_ACQUIRE_TIMEOUT_SECONDS"
        }
      ]
    }
  ]
}
