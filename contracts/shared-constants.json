{
  "contract": "shared-constants",
  "version": 1,
  "readme": "contracts/README.md",
  "description": "少数几个「两个运行时都要认、而谁也不拥有」的常量。不一致时没有任何报错 —— 只是行为悄悄错开,所以每一条都写清了错开之后用户会看到什么。",
  "notes": [
    "只放**没法单向下发**的常量。能由一侧算出来再传给另一侧的(例如面板圆角经 IPC 下发)不属于这里 —— 那种本来就只有一份。",
    "常量说的是「两边必须**相等**」;`budgets` 说的是「一边必须**装得下**另一边」—— 跨进程的时间预算属于后者,而这份契约里此前一条都没有。它们错开时同样没有任何报错,只是等待在两侧的含义悄悄不同。"
  ],
  "constants": [
    {
      "name": "publish_partition_prefix",
      "value": "mosael",
      "why": "发布账号的登录分区名(persist:<前缀>-<accountId>)。两边拼的是**同一个磁盘目录**:后端按它清理/迁移账号目录,Electron 按它创建会话分区。改了其中一个,所有已登录的发布账号的 cookie 会凭空消失,用户得重新登录每一个平台 —— 而且不会有任何报错。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/core/db.py",
          "symbol": "PARTITION_PREFIX"
        },
        {
          "runtime": "electron",
          "location": "electron/publish/accountViews.ts",
          "symbol": "PARTITION_PREFIX"
        }
      ]
    },
    {
      "name": "embed_header_height_px",
      "value": 56,
      "why": "内嵌发布视图从顶部这么多像素处开始铺,上方那条留给「登录中 · 返回」工具栏。Electron 按它定视图边界,前端按它定那条栏的高度。两者不等就会露出一条缝,缝里是 App 自己的顶栏 —— 看着像画面穿帮,而不像一个 bug,所以很容易被当成本来就长这样。原生窗口按钮、应用和素材对比顶栏也共用这一高度,避免中心偏移。",
      "implementations": [
        {
          "runtime": "electron",
          "location": "electron/publish/types.ts",
          "symbol": "EMBED_HEADER_HEIGHT"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/app/App.tsx",
          "symbol": "PUBLISH_BAR_HEIGHT"
        },
        {
          "runtime": "electron",
          "location": "electron/window-chrome.cjs",
          "symbol": "WINDOW_CHROME_HEIGHT"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/lib/windowChrome.ts",
          "symbol": "WINDOW_CHROME_HEIGHT"
        }
      ]
    },
    {
      "name": "note_append_separator",
      "value": "\\n\\n",
      "why": "后端把追加的内容用它接到笔记正文后面;前端**按同一个串判断一次改动是不是纯追加**,是才把它并进正在编辑的草稿(见 features/notes/appendMerge)。两边不等时不会报错 —— 前端只是再也认不出追加,于是每次从逐字稿/字幕导出都把正在写作的人推进冲突条,而那看起来像是「协作就是这么难用」,不像一个可修的 bug。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/notes.py",
          "symbol": "APPEND_SEPARATOR"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/features/notes/appendMerge.ts",
          "symbol": "APPEND_SEPARATOR"
        }
      ]
    },
    {
      "name": "credential_lease_ttl_seconds",
      "value": 30,
      "why": "OAuth 凭据刷新租约的 TTL。后端按它判断「持有者是不是死了」,sidecar 按它算续租间隔(取三分之一)。sidecar 把它猜小了会白白多续几次(无害),猜大了则在真正的 TTL 过去之后才续——租约被别人顶替,于是**两个 sidecar 同时拿同一份一次性 refresh token 去刷新**,先手刚存好的凭据当场失效,用户看到「刚登录就被登出」,偶发且难复现。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/provider_auth.py",
          "symbol": "LEASE_TTL_SECONDS"
        },
        {
          "runtime": "agent-sidecar",
          "location": "agent-sidecar/src/credentials.ts",
          "symbol": "CREDENTIAL_LEASE_TTL_SECONDS"
        }
      ]
    },
    {
      "name": "credential_acquire_timeout_seconds",
      "value": 20,
      "why": "后端 `acquire_lease` 等别人释放的上限,超过才返回 409。**sidecar 必须知道这个数**:它收到一个 409 意味着已经等过这么久,而不是「几秒」。早先 sidecar 的注释写的是「它几秒内会结束」,于是按 400/800/1200ms 退避重试 3 次,以为自己总共等了 2.4 秒 —— 实际这一轮对话在凭据上最多站住约 80 秒。两侧对同一段等待的理解差了三十倍,而没有任何一处同时看得见这两个数。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/provider_auth.py",
          "symbol": "ACQUIRE_TIMEOUT_SECONDS"
        },
        {
          "runtime": "agent-sidecar",
          "location": "agent-sidecar/src/credentials.ts",
          "symbol": "CREDENTIAL_ACQUIRE_TIMEOUT_SECONDS"
        }
      ]
    },
    {
      "name": "board_item_title_max_chars",
      "value": 120,
      "why": "画板上一格的名字(title)最长多少字。后端按它校验每一次保存,前端按它给改名输入框设 maxLength。前端放得比后端长时不会在输入那一刻报错 —— 名字照打,而下一次自动保存整张画板被拒,用户只看到「画板没能保存」,和刚才改的名字对不上号。",
      "implementations": [
        {
          "runtime": "python",
          "location": "backend/app/domain/boards/canvas.py",
          "symbol": "MAX_TITLE_CHARS"
        },
        {
          "runtime": "frontend",
          "location": "frontend/src/features/boards/BoardNodeLabel.tsx",
          "symbol": "BOARD_ITEM_TITLE_MAX"
        }
      ]
    }
  ],
  "budgets": [
    {
      "name": "confirmation_wait_fits_inside_the_turn",
      "rule": "less_than",
      "why": "一张确认卡最多等多久,必须压在后端那一轮的总时限底下。超过它:卡还亮着,而那一轮已经被判超时 —— 用户点了批准,什么都不会发生,两侧都不报错。这层关系此前只写在 tools.ts 的一段注释里,而改 Python 那个 600 的人没有任何理由来读一段 TypeScript 注释。",
      "inner": {
        "runtime": "agent-sidecar",
        "location": "agent-sidecar/src/tools.ts",
        "symbol": "CARD_WAIT_CEILING_MS",
        "unit": "ms"
      },
      "outer": {
        "runtime": "python",
        "location": "backend/app/ai/sidecar/adapters.py",
        "symbol": "TURN_TIMEOUT_SECONDS",
        "unit": "s"
      }
    },
    {
      "name": "plugin_call_fits_inside_the_tool_call",
      "rule": "less_than",
      "why": "后端跑一次插件工具的预算,必须装得进 sidecar 等一次工具调用的预算。反过来的话 sidecar 早就放弃了,而后端还在跑 —— 它占着的那份资源(插件子进程、独占锁)没人知道要等多久,而模型收到的是一句超时,于是它会重试,把同一件事再做一遍。",
      "inner": {
        "runtime": "python",
        "location": "backend/app/domain/plugins/runtime.py",
        "symbol": "PLUGIN_TIMEOUT_SECONDS",
        "unit": "s"
      },
      "outer": {
        "runtime": "agent-sidecar",
        "location": "agent-sidecar/src/tools.ts",
        "symbol": "TOOL_CALL_TIMEOUT_MS",
        "unit": "ms"
      }
    },
    {
      "name": "mcp_call_fits_inside_the_tool_call",
      "rule": "less_than",
      "why": "同上,MCP 那条传输的预算。两条并列而不是合成一条:它们是两种传输,各自可以有各自的理由变,而**都要装进同一个外层**。",
      "inner": {
        "runtime": "python",
        "location": "backend/app/domain/plugins/mcp_bridge.py",
        "symbol": "MCP_TIMEOUT_SECONDS",
        "unit": "s"
      },
      "outer": {
        "runtime": "agent-sidecar",
        "location": "agent-sidecar/src/tools.ts",
        "symbol": "TOOL_CALL_TIMEOUT_MS",
        "unit": "ms"
      }
    },
    {
      "name": "blender_agent_step_fits_inside_the_mcp_client",
      "rule": "less_than",
      "why": "智能体的 blender_inspect / look / execute 由 MCP 客户端调用,而 mcp_server 给它的 HTTP 预算是 180 秒。后端等得比调用方久没有任何意义:对方早就放弃了,而我们还占着那把独占锁 —— 用户的下一次操作被挡在外面,且没有人说得出为什么。(Blender **场景互通**那条是另一个数,它由界面发起,上面没有更短的预算压着。)",
      "inner": {
        "runtime": "python",
        "location": "backend/app/domain/blender/bridge.py",
        "symbol": "BLENDER_AGENT_TIMEOUT_SECONDS",
        "unit": "s"
      },
      "outer": {
        "runtime": "python",
        "location": "backend/mcp_server.py",
        "symbol": "BLENDER_CLIENT_TIMEOUT_SECONDS",
        "unit": "s"
      }
    }
  ]
}
