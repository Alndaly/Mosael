.DS_Store
.venv/
__pycache__/
*.pyc
node_modules/
dist/
build/
.vite/
*.db
*.db-shm
*.db-wal
.env

release/
backend/build/
backend/dist/
*.spec

# App icon sources for electron-builder packaging (root build/ is otherwise ignored)
!/build/
/build/*
!/build/icon.png
!/build/icon.icns
!/build/installer.nsh

# esbuild 产物(build:publisher / watch:publisher 现生成)
electron/publish.bundle.cjs
electron/system.bundle.cjs
.claude/

# TS 增量构建产物,每次构建都变
*.tsbuildinfo

# 随 App 分发的独立 CPython(构建期由 scripts/fetch-tts-python.mjs 抓取)
/build/python/
