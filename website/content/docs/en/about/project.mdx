---
title: About the project
description: Find the author, source license, releases and credits for demonstration media.
order: 1
updated: 2026-09-09
version: 1.5.0
---

## Source repository

The source code is hosted on GitHub: **[Alndaly/Mosael](https://github.com/Alndaly/Mosael)**.

- Desktop installers: [GitHub Releases](https://github.com/Alndaly/Mosael/releases) (macOS arm64 `.dmg` / Windows x64 installer, built and uploaded by CI on every tag)
- Bug reports / feature requests: [GitHub Issues](https://github.com/Alndaly/Mosael/issues)

## Author

**Kinda Hall** (GitHub [@Alndaly](https://github.com/Alndaly) · X [@KindaHuax](https://x.com/KindaHuax)).

## License

Mosael is **source-available, not open source**, under a proprietary license (full text in the repository's [LICENSE](https://github.com/Alndaly/Mosael/blob/main/LICENSE)):

- Allowed: viewing the source, building and running locally for evaluation, learning and **personal non-commercial** use.
- Not allowed: any commercial use (selling, paid services, SaaS hosting, embedding in commercial products) or redistribution.
- Commercial licensing: contact the copyright holder via GitHub for a separate written license.

## Tech stack

| Layer | Technology |
| --- | --- |
| Desktop shell | Electron (with a built-in publish executor driving embedded browser views for logins / uploads) |
| Frontend | Vite + React + TypeScript + Tailwind |
| Backend | FastAPI (Python) + SQLite, single source of truth; packaged with PyInstaller |
| AI agent | Embedded Node.js sidecar based on pi-agent-core / pi-ai, using Mosael tools |
| Docs site | Next.js (this site) |

All user data lives locally in `~/.mosael` — no cloud dependency.

<h2 id="media-credits">Capture notes and media credits</h2>

Documentation targets 1.5.0. Unchanged captures retain their original provenance. The 1.5.0 changes are not recaptured yet: the new Post and Published rows in the publish detail (the post ID and View on the platform); the author byline under a plugin's title and the Docs button that now appears only when the plugin has a guide; the plugin market's search box and Install from link at the app's standard size; the workflow community dialog with its list and detail scrolling separately; the note under the admin account list, now a hover hint on the sole admin's switch; home project cards using the timeline's first frame as cover; and the publish node's new Post ID and Post link outputs. The 1.4.3 changes are not recaptured yet: the new “auto-approved N times in this chat” section in the agent's session settings (listing which tier let each action through) and the “someone else set this mode” note under the permission picker; the statistics page's **AI usage** tile now showing money instead of a call count, and the per-provider breakdown row under the cost chart; the publish page's new banner for a queued publish with no desktop executor running; the 3D scene page's object list as a real tree (groups nest and collapse), the new view-axis gizmo in the viewport corner, the shader-drawn ground grid and one-click fold/unfold of groups; the client column on the admin page (a browser-extension session now reads “Extension v0.1.0” instead of “vbrowser-extension”); and an answered choice drawn as a single card in the transcript (the receipt and the result used to appear side by side). 1.4.2 changes are not recaptured yet: the new **模型库** group in the 3D scene page's add menu (models already in this workspace listed alongside the primitives, one click to place); the new **Available 3D props** node on the workflow canvas and its multi-select model picker; the **Stop** button the workflow toolbar shows while a run is in flight, and the new **Cancel task** button in the run detail dialog; the sub-task rows in that dialog that now expand in place to show their progress; the new **supports json_schema** switch in model settings and its explanatory copy; the agent's model picker, which now holds its slot while loading or when one connection fails; the five new official workflow templates (four business-facing ones plus “Montage from your own footage · voice-over and subtitles”); the “bound workflow deleted” row in task details, which is no longer a clickable button; the bubble of a failed turn, which now keeps its tool cards and partial text above the error card; and the “what you picked” card an answered choice now leaves in the transcript (it used to be a line of text). The 1.4.1 changes are not recaptured either: the hatched “media offline” placeholder on the timeline, the offline frame in the monitor and the matching notice in the inspector; “Split here” and “Save this frame” now in the timeline toolbar (the row above the monitor is gone, so the editing captures still show the old layout); the new “Max output tokens” field in model settings, the source notes for both limits and the thinking-level hint; the new “permanent delete” badge on confirmation cards; the re-coloured reference chips in agent messages and the “Add reference” button in the composer; the new “Build a 3D blockout” and “Render a blockout reference” nodes on the workflow canvas, the “Topic to finished video” template at v6 and the new asset-group picker on generation nodes; the resizable inspector and agent panes on the 3D scene page; and the whole surface of the agent modelling inside Blender (viewport frames in tool results and the images view_scene brings back). The 1.4.0 changes are not recaptured either: the Generate subtitles and Dub subtitles nodes and the “Translated dubbing with subtitles” official template on the workflow canvas, 3D scene card thumbnails and list alignment, the note editor's code blocks and error notice, the quality note on URL import, the agent's context meter and confirmation-card wording, and in Settings the “Where parameters come from” picker and “Describe this endpoint” parameter-set form, the model-list placeholder shown while a connection expands, the queued-message strip above the agent composer, the regrouped Settings page, the separation engine page and asset action, the AI Studio Audio tab, the subtitle dubbing form now on the editor's Voice tab (the dubbing captures still show the old popover on the Subtitles tab), the noise reduction dialog and Settings → Noise reduction page, the transcript cleanup canvas with its new denoise node (the workflows capture still shows the older graph), the four-way “original audio” field on the dubbing form, the node inspector's dropdowns now showing readable option names, and the task centre that announces a run once. The 1.3.0 searchable dropdowns, team-discussion sheet and composer attachments, together with the 1.3.1 @-references, thinking levels, generation engine-settings panel and menu separators, are not recaptured either, so those frames still show 1.2.0. Captures are taken directly in an isolated demo workspace in Chinese/English and light/dark themes. Subtitles are manually prepared exercise text; narration uses system speech synthesis. Empty agent and publishing views mean no run has occurred, not a successful AI result or platform post.

Footage and frames are from **Big Buck Bunny**, © 2008 Blender Foundation / www.bigbuckbunny.org, used under [CC BY 3.0](https://creativecommons.org/licenses/by/3.0/). The demo trims, resizes and rearranges excerpts; see the [film and license information](https://peach.blender.org/about/).

The [capture guide and media inventory](https://github.com/Alndaly/Mosael/blob/main/docs/media/README.md) describe sources, scenes and repeatable recording.
