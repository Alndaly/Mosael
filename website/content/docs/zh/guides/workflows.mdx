---
title: 可视化工作流
description: 节点式编排：节点、连线、变量、连接输入、就绪检查、触发器、AI 搭建。
order: 4
---

工作流把一串动作(检索、生成、转写、导出、发布等)编排成一张**有向无环图**，可手动 / 定时 / Webhook 触发。设计思路取自 Dify / Coze。

## 画布与节点

![工作流画布：检索风格指南 → 生成口播文案 / 起标题 → 拼装发布文案 → 通知](/media/screens/workflows.png)

**从零搭一条工作流**——「添加节点」放入「知识库检索」「LLM 生成」，拖动端口连线；右侧就地配置节点参数，画布还内置 AI 助手可以代劳搭建：

![操作演示：添加节点、连线、配置 LLM 生成节点](/media/gifs/workflows.gif)

- 左栏是工作流列表，右侧是 React Flow 画布 + 顶部工具栏。
- **添加节点**：工具栏「添加节点」选类型。类型包括：开始、LLM 生成、知识库检索、插件工具、素材转写、导出时间线、AI 生成素材、发布、条件分支、HTTP 请求、代码、文本模板。
- **连线**表示执行顺序；从节点右侧接点拖到下游节点左侧接点。**条件分支**节点有「真 / 假」两个出口。
  工具栏可切**连线形态**：曲线(贝塞尔)或折线(直角)，按你的偏好记住。
- 点节点打开右侧**属性面板**(头部是节点图标 + 可内联改名 + 类型)。

### 挡住了怎么办

节点密集或悬浮面板叠在一起时，选中后按 **⌘/Ctrl + ]** 提到最上、**⌘/Ctrl + [** 压到最下。
悬浮面板和画布节点用同一组快捷键，但**节点永远不会盖住悬浮面板**——不然属性面板会被自己正在
编辑的节点埋掉。

## 变量：把上游输出接到下游输入

节点执行后把结果写进上下文，输出变量形如 `{{节点id.输出名}}`(如 `{{llm-1.text}}`)。下游字段用它引用上游数据。

每个字段可在**两种输入方式**间切换(标签行右侧的小胶囊):

- **手动**：直接填值 / 选项；模板字段支持 `/` 唤起变量、或点下方变量 chip 插到光标处。
- **连接**：字段变成一个接点，从**上游输出**下拉里选一个，等价于把 `{{上游.输出}}` 绑上去。切回手动会保留原值，不丢数据。

## 就绪检查 / 运行门禁

工具栏「**就绪检查**」聚合所有阻断项：缺必填、模型 / 服务商没配、节点未连接到流程、引用了已删除的节点等。

- 画布上有问题的节点挂**告警角标**(红 = 阻断，琥珀 = 提醒),tooltip 列出问题。
- 有 error 时「运行」按钮禁用；每条问题可点击直达对应节点。
- 删了上游节点后，引用它的字段会标为**失效引用**，可「重新指向」到别的上游输出或移除。

## LLM 生成风格

LLM 节点用**生成风格**(精确 / 均衡 / 创意)替代裸 temperature，降低配置门槛。

## 触发器

工作流可绑到「定时任务」上，触发方式：手动、定时(cron)、Webhook。也支持**批量**跑同一工作流。

<Aside type="caution" title="定时任务需要应用在运行">

定时调度跑在本机后端里，而后端是 Open Studio 的子进程 —— **完全退出应用，定时任务就不会触发**。

所以关窗只是把它收进托盘、并不退出；要真正退出请用托盘菜单或 ⌘Q。想让它开机就守着，在
设置 → 本地后端里打开「开机时启动」。退出期间错过的任务会在下次启动后补跑一次(按当前时间
重算下次时间，不会把错过的每一次都堆起来跑)。

</Aside>

## 让 AI 帮你搭

工具栏「AI 助手」是一个持久会话：用自然语言描述你想要的流程，它来搭建 / 修改节点；改动先出确认卡等你批准。
