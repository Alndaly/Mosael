---
title: 关于项目
description: 了解作者、源码许可、版本下载与演示素材来源。
order: 1
updated: 2026-09-09
version: 1.4.3
---

## 源码地址

源码托管在 GitHub:**[Alndaly/Mosael](https://github.com/Alndaly/Mosael)**。

- 桌面安装包：[GitHub Releases](https://github.com/Alndaly/Mosael/releases)(macOS arm64 `.dmg` / Windows x64 安装器，推 tag 后由 CI 自动打包上传)
- 问题反馈 / 功能建议：[GitHub Issues](https://github.com/Alndaly/Mosael/issues)

## 作者

**Kinda Hall**（GitHub [@Alndaly](https://github.com/Alndaly) · X [@KindaHuax](https://x.com/KindaHuax)）。

## 许可

Mosael **源码开放但非开源软件**，采用专有许可(全文见仓库根目录 [LICENSE](https://github.com/Alndaly/Mosael/blob/main/LICENSE)):

- 允许：出于评估、学习与**个人非商业**用途查看源码、本地构建与运行。
- 禁止：任何商业用途(售卖、付费服务、SaaS 托管、集成进商业产品)与再分发。
- 商业授权：请通过 GitHub 联系版权人另行取得书面许可。

## 技术栈

| 层 | 技术 |
| --- | --- |
| 桌面壳 | Electron(内置发布执行器，驱动内嵌浏览器登录 / 上传) |
| 前端 | Vite + React + TypeScript + Tailwind |
| 后端 | FastAPI(Python)+ SQLite，单一事实源；PyInstaller 打包进应用 |
| AI 智能体 | 内置 Node.js 边车，基于 pi-agent-core / pi-ai，调用 Mosael 工具 |
| 文档站 | Next.js（本站） |

默认工程数据保存在本机 `~/.mosael`；连接远程后端后则保存在该服务器，浏览器登录分区仍属于桌面执行器。

<h2 id="media-credits">实拍演示与素材署名</h2>

文档适用于 1.4.3。未变更画面保留较早的实拍与逐文件来源记录。1.4.3 改过的界面尚未补录：智能体会话设置弹层里新增的「这次对话自动放行了 N 次」一栏（列出被哪一档放行的每一条），以及权限档选择器下方新增的「这一档是别人开的，对你不生效」提示；统计页「AI 用量」磁贴改成显示金额（此前是调用次数），费用图下方新增的「近 14 天按供应商分摊」一行；发布页在排队中而桌面执行器没起来时新增的那条横幅；3D 场景页物体列表改成的树形结构（组能套组、能整组收起）、视口右上角新增的坐标轴控件、改成着色器绘制的地面网格，以及一键收拢/展开分组；管理页里客户端那一栏（浏览器扩展现在显示「浏览器扩展 v0.1.0」，此前是「vbrowser-extension」）；以及答完的选择在对话里只画一张卡（此前回执和选择结果并排出现两次）。1.4.2 改过的界面尚未补录：3D 场景页「添加」菜单里新增的「模型库」一组（这个工作区已有的模型和几何体并排列出来，选一个就摆上）；工作流画布上新增的「可用的 3D 道具」节点，以及它那一格多选模型的控件；工作流工具栏在运行时变成的「停止」按钮，和任务执行详情弹窗左下角新增的「取消任务」；任务详情里可以就地展开看过程的子任务行；模型设置弹窗里新增的「支持 json_schema」开关与它的说明文案；智能体顶部在读取中或某条连接出错时仍然占着那一格的模型选择器；工作流列表里新增的五个官方模板（四个面向生意的，加一个「自有素材混剪 · 配音与字幕」）；任务详情里「绑定的工作流已删除」那一格 —— 它不再是个能点的按钮;失败的那一轮气泡 —— 已经做过的工具卡和正文现在留在错误卡上方;以及答完的选择在对话里画成的那张「你选了哪一项」卡片(此前是一行「我选好了:…」)。1.4.1 改过的界面同样尚未补录：时间线上「素材已删除」的斜纹占位、监视器上的脱机红屏与检查器里对应的提示；搬进时间线工具栏的「在此切一刀」与「存下这一帧」（监视器上方那一行已去掉，剪辑一节的截图仍是旧布局）；模型设置弹窗里新增的「最大输出 Token」、两个上限的来源说明与思考档位提示；确认卡上新增的「永久删除」档徽标；智能体消息里改过底色的引用胶囊与输入框上的「引用文档」按钮；工作流画布上新增的「搭建 3D 白模场景」「渲染白模参考」两种节点、升到 v6 的「从主题到完整视频」官方模板、生成节点上新增的「用哪一组素材」选择；3D 场景页可拖宽的属性栏与智能体栏；以及智能体在 Blender 里建模的整条界面（工具结果里的视口画面与 view_scene 取回的图）。1.4.0 改过的那批同样尚未补录：工作流画布上的「生成字幕」「字幕配音」两种节点与「视频译配 · 字幕与配音」官方模板、3D 场景列表的卡片缩略图与对齐、笔记编辑器的代码块与错误提示、链接导入的画质说明、智能体的上下文占用条与确认卡措辞，以及设置页里的「参数按什么来」选择器与「描述这个端点」参数组表单、连接展开时的模型列表占位、智能体输入框上方的排队条，以及按用途重新分组的设置页、人声分离的安装页与素材入口、AI Studio 的「音频」页、剪辑台里搬进「配音」页的字幕配音表单（配音一节的截图仍是字幕页弹层的旧样子）、降噪对话框与「设置 → 降噪」页，以及多了一个降噪节点的「口播与访谈智能整理」画布（工作流一节的截图仍是旧版）、配音表单上新增的「原声」四档下拉、节点检查器里改成中文名的下拉选项，以及只报一次的任务中心。1.3.0 的下拉搜索、团队讨论侧栏、智能体输入框附件，以及 1.3.1 的 @ 引用、思考档位、生成页引擎参数栏和菜单分隔线同样尚未补录，相关画面仍是 1.2.0 的实拍。素材分别覆盖中文、英文与浅色、深色主题。演示工程的字幕是手工准备的练习文本，旁白由系统语音合成；空白智能体和发布页表示尚未运行，不代表 AI 生成或平台发布成功。

画面片段与截帧来自 **Big Buck Bunny**，© 2008 Blender Foundation / www.bigbuckbunny.org，按 [CC BY 3.0](https://creativecommons.org/licenses/by/3.0/) 使用。演示中截取、缩放并重新编排了原片；[影片与许可说明](https://peach.blender.org/about/)。

[录制脚本与媒体清单](https://github.com/Alndaly/Mosael/blob/main/docs/media/README.md)说明场景、来源与复录方式。
