/** Bilingual highlights for major releases; publication status always comes from GitHub. */
export const releaseCopy: Record<string, { zh: string[]; en: string[] }> = {
  "v1.5.1": {
    zh: ["**大录屏在应用里拖得动了。** Mac 录屏的 `.mov` 导入时原样换成 `.mp4` 容器(不重新编码、画质不变),库里已有的自动换;一条 370MB 的录屏单次跳转从最多 14.5 秒降到约 0.1 秒,进度条拖动也跟手了。", "**摄像头录得清。** 录制器授权后就显示实时画面,开录沿用这路已经调好曝光的画面 —— 开头不再发黑,镜像录制也不再定格。", "**一次导入一批。** 素材页和剪辑页都能多选文件导入;标签筛选可同时勾几个;视频也能对比,几条一起播、逐帧对照。", "首页项目可以批量删除,「新建项目」先起名、建好留在首页;从链接导入的分 P 各带标题,取不到时说清是不支持、没有还是要登录。"],
    en: ["**Large screen recordings scrub smoothly.** QuickTime `.mov` files are repackaged into `.mp4` as they are imported (no re-encode, same quality), and existing ones are converted automatically; a single seek in a 370 MB recording went from up to 14.5 s to about 0.1 s, and the progress bar now follows your drag.", "**Clean camera recordings.** Once access is granted the recorder shows a live camera preview and recording takes over that already-exposed stream — no dark opening, and mirrored recordings no longer freeze.", "**Import in batches.** Pick several files at once on the Media page and in the editor; filter by several tags; compare videos side by side, playing together and stepping frame by frame.", "Delete projects in bulk on Home, name a new project before it is created, see per-part titles when importing from a link, and get a clear reason (unsupported, nothing there, sign-in needed) when a link yields nothing."],
  },
  "v1.5.0": {
    zh: ["**发布完成后记下那条作品在平台上叫什么。** 执行器从平台自己的发布接口的返回里读出作品 ID(抖音 / TikTok、B 站、小红书、YouTube),连同链接和发布时间存进任务:发布详情里点得开,工作流的发布节点多了「作品 ID」「作品链接」两个输出,智能体也读得到 —— 之后按作品查数据(TikHub 之类)就靠它。没读到就明说,不编。", "**腾讯云 COS 官方插件**:上传、取限时直链、拉回素材库、列目录,本地素材自动变成公网链接。签名用 COS 原生的 `q-sign`,和官方 SDK 逐字对过。", "**Remotion 官方插件:用代码做动画。** 知识点讲解、公式推导、代码演示 —— 给内容就出一段讲解视频,更自由的画面写一段组件,零生成费用。", "**插件写得出作者和使用文档**:插件页和市场里看得到是谁写的;「文档」跳到这个插件自己的那一页。", "**画板上失败过的节点,重新生成不再「点了没反应」**:后端照常提交(照常计费),画布却挂着上一轮的失败,很容易再点一次。3D 场景一键生成的视频也不再照着白模画 —— 提示词说清了白模只是占位、画面里有什么。", "**本地素材换公网链接只用你自己的存储**:此前多人部署时会用到别人的桶和密钥;配了几家时在「设置 → 素材外链」里选一家,同一份素材链接没过期就复用。", "**选中文字能复制了**:工作流画布和剪辑台此前把 ⌘C 拿去复制节点 / 片段。首页封面改取时间线上最早出现的画面;阿里云 OSS 的签名和端点问题修好了;下拉框在窗口顶部点得中了。", "全部依赖升到最新:Electron 44、Python 3.14、Node 24 LTS。官网的插件与工作流改成社区式的浏览与详情页,下载分国内(百度网盘)/ 海外(GitHub)两条路。"],
    en: ["**Mosael now records the post each publish created.** The executor reads the post ID from the platform's own publish response (Douyin / TikTok, Bilibili, Xiaohongshu, YouTube) and stores it with the link and time: open it from the publish detail, wire the publish node's new Post ID / Post link outputs into the next step, or let the agent read it — the basis for looking the post up later (TikHub and the like). When it could not be read, it says so instead of inventing one.", "**Tencent Cloud COS official plugin**: upload, time-limited links, pull back into the library, list — local assets become public links automatically. Signing uses COS's native `q-sign`, checked byte for byte against the official SDK.", "**Remotion official plugin: animation from code.** Lesson explainers, formula walk-throughs, code demos — give it the content and get a video, or write a component for anything freer, at no generation cost.", "**Plugins can name their author and their guide**: the plugin page and market show who wrote it, and Docs opens that plugin's own page.", "**Regenerating a failed board node no longer looks like nothing happened**: the job was submitted (and billed) while the canvas kept showing the previous failure, inviting a second click. One-click video from a 3D scene no longer copies the grey blockout — the prompt now says the blockout is a placeholder and lists what is in the scene.", "**Local assets are linked through your own storage only**: on shared deployments someone else's bucket and keys could be used before. With several set up, pick one under Settings → Asset links; the same asset reuses its link until it expires.", "**Selected text copies again** — the workflow canvas and the editor had taken ⌘C for nodes and clips. Home covers use the first frame on the timeline; Aliyun OSS signing and endpoints are fixed; dropdowns near the top of the window are clickable.", "Every dependency is on its latest release: Electron 44, Python 3.14, Node 24 LTS. The website's plugins and workflows are now community-style browse and detail pages, and downloads split into China (Baidu Netdisk) and global (GitHub)."],
  },
  "v1.4.3": {
    zh: ["这一版没有新的大功能:它把五路只读审计找出的 **55 条**问题逐条修完了。那 55 条有一个共同点 —— **它们都不报错**:代码在跑、测试是绿的、界面看着正常,而某件事已经悄悄不对了。", "**对话永久停在「思考中」**修好了:收尾时先撤看门狗再无限期等子进程,于是子进程一旦不退,那一轮再也回不来。按「停止」时子智能体也会真的停;插话和停止要等对面回一句「接住了」,不再把「字节写出去了」当成「对面接住了」。", "**AI 视频的参考素材:本地文件现在会自动传成公网直链。** 部分供应商(如方舟 Seedance)的参考视频只收 http(s) 链接,此前给本地素材会收到一个英文 400。同时新增**阿里云 OSS、亚马逊 S3、火山引擎 TOS** 三个官方插件,用来上传、下载和取直链。", "**工作流不再先花掉钱、再告诉你少配了什么。** 缺必填项如果在循环体里,启动前的检查看不见它 —— 「从主题到完整视频」会把创意主旨、脚本、视觉圣经、分镜、搭白模全跑一遍(好几次付费调用),才死在循环上。现在启动前就拦住,并指出是哪个循环里的哪个节点。", "**降级不再一声不吭**:模型不支持结构化输出或思考档位时会自动降一档,此前三处触发、零处记录。「最大输出 Token」和思考档位在直连模型上也终于发得出去了 —— 此前八个调用点都没带上,而「发不出思考档位」被当成了「这个模型不会思考」。", "**自动放行有了痕迹**:会话设置里列出这次对话被自动放行过哪些操作、各是被哪一档放的;权限档是别人开的时候会明说「对你不生效」。", "**首页「AI 用量」磁贴显示的是钱,不是调用次数**,费用图下面多一行按供应商的分摊。发布页会说「桌面执行器没在运行」—— 此前排队中的发布不会开始,而界面一个字都不说。", "**3D 场景**:物体列表改成真正的树(组能套组、能收起)、视口右上角加了坐标轴控件、地面网格改成着色器绘制、一键收拢/展开分组。Blender 大场景不再误报「未响应」——互通此前借用了插件那条 60 秒的通用预算。", "**浏览器扩展改说你的语言**(此前固定按中文向后端要文案);删除一个还被引用的东西统一成「拒绝 + 点名谁在用」;时间线算子从 10 个补到 19 个 —— 此前智能体能用的编辑动作只有清单上的一半。", "**升级这条路第一次真的被跑过**,当场抓到一条启动就炸的迁移顺序问题。后端重启后,插件调用记录和 Blender 互通记录不再永远停在「进行中」;sidecar 在后端消失后会跟着退出,不再留下没人管的进程。"],
    en: ["No new headline feature this time: it closes all **55 findings** from five read-only audits. Those 55 share one trait — **none of them raised an error**: the code ran, the tests were green, the UI looked fine, and something was already quietly wrong.", "**Conversations stuck on “thinking” forever** are fixed: teardown cancelled the watchdog before waiting indefinitely on the child process, so a child that would not exit took the turn with it. Stop now really stops sub-agents, and steering/abort wait for the sidecar to acknowledge instead of treating “bytes written” as “received”.", "**Local files are now uploaded to a public link automatically** when used as a video reference. Some providers (e.g. Volcengine Seedance) only accept http(s) URLs for reference video; a local asset used to come back as an English 400. Three official storage plugins ship with it — **Aliyun OSS, Amazon S3 and Volcengine TOS** — for upload, download and public links.", "**Workflows no longer spend your money before telling you what is missing.** A required field inside a loop body was invisible to the pre-run check, so “Topic to finished video” would run the brief, script, visual bible, storyboard and blockout (several paid calls) before dying at the loop. It is now refused up front, naming the loop and the node.", "**Fallbacks are no longer silent**: dropping a tier because a model cannot do structured output or a thinking level used to fire in three places and be recorded in none. Max output tokens and thinking level are finally sent on directly connected models too — eight call sites omitted them, and “cannot send a thinking level” was being read as “this model cannot think”.", "**Auto-approval leaves a visible trace**: session settings list which actions this conversation auto-approved and which tier let each one through, and a mode set by someone else now says so instead of looking enabled.", "**The home “AI usage” tile shows money, not call count**, with a per-provider breakdown under the cost chart. The publish page now says when the desktop executor is not running — queued publishes simply never started, and nothing on screen said why.", "**3D scenes**: the object list is a real tree (groups nest and collapse), a view-axis gizmo sits in the corner, the ground grid is shader-drawn, and groups fold with one click. Large Blender scenes no longer report a false “not responding” — the bridge had been borrowing the plugin runtime's generic 60-second budget.", "**The browser extension speaks your language** (it used to always ask the backend for Chinese). Deleting something still referenced is now consistently refused with the referrers named. Timeline operations went from 10 to 19 — the agent could previously use only half the list.", "**The upgrade path was exercised for the first time**, catching a migration ordering bug that crashed startup. After a restart, plugin invocation records and Blender transfers no longer sit at “in progress” forever, and the sidecar exits with the backend instead of lingering."],
  },
  "v1.4.2": {
    zh: ["白模参考帧里**终于画得出你自己的模型**。在 Blender 里建好的产品、道具、设备,收进 Mosael 之后,此前只在 3D 工作台里看得见 —— 自动流程渲给图像和视频模型的每一张构图参考里,那件东西是空的,而两边都不报错。现在它会被画进去,颜色取它自己的材质;真渲不动的(Draco/meshopt 压缩网格、面数超预算、文件不在)会逐条告诉你是哪一件、为什么。", "导入的 3D 模型改成**属于工作区,不属于某个场景**:一件道具导一次,哪个场景都摆得上,「添加」菜单里多了一组「模型库」。此前它挂在场景下面,而工作流每跑一次都新建一个场景 —— 于是 Blender 建的模型永远进不了自动成片。删场景不再删模型;删模型本身时,还有场景在用会明确拒绝并说出是哪几个。", "工作流新增「**可用的 3D 道具**」节点:把工作区里的模型列成「名字 + 实测长宽高」交给布景师,自动搭出来的白模布景里就能摆真道具,而不是只用方块和圆柱拼。尺寸是读模型量出来的,不是让你填的。", "智能体在 Blender 里看模型不再只有六个固定角度:可以指定任意方位角和仰角、把镜头凑近、只框住刚做完的那个部件,还多了一档「看穿」用来检查两件东西是不是真的穿在一起 —— 整场景取景下一处接缝只有几个像素,那时它和没建出来长得一模一样。", "**一轮失败不再抹掉这一轮做过的事**。一次跑了三分钟、调了十来次工具的对话,最后一步断线,此前看到的只剩一句「智能体执行失败」 —— 而那几次工具调用真的发生过。落库时就没存,界面上还要再丢一次。现在过程在上、失败原因在下。此前这是最糟的一种不对称:模型的记忆回存了,给人看的记录没有。", "答完的选择卡在对话里**还是一次选择**:此前它被拼成一句「我选好了:…」当用户消息发回去,问题和选项的结构全丢了,看起来还像是你手打的。现在界面画回「问的是什么、你选的是哪一项」。", "**跑起来的工作流能停下了**。此前能取消的地方只有任务中心列表那一行的 ×:点开详情看着进度条,那里没有任何出口,工作流页的运行按钮在跑的时候也还是一颗 ▶。现在工具栏变成「停止」,详情弹窗里也有「取消任务」;中止是节点粒度的,当前这一步跑完后停下,已经产生的结果都保留。", "新增五个官方模板:四个面向生意的(高光短视频、产品上身、卖点短片、面料图册),以及企业最常见的「**自有素材混剪 · 配音与字幕**」—— 手上已经有素材,只要混剪、配音、字幕。分镜数加了成本闸门,一次生成不会悄悄变成几十镜。", "结构化输出不再撞运气:没给 Schema 的 `json_object` 节点此前必然 400;模型返回的 JSON 不合格时,现在先把错在哪说清楚,再给它一次改的机会。模型设置里新增「支持 json_schema」,并对查证过的供应商给出结论。", "修复无限画布整页点不动 —— React Flow 的样式表跟着按需加载的页面一起走丢了。以及按需加载留下的三处后遗症,和两处把「还不知道」当成「结论」显示的地方(智能体的会话列表、任务绑定的工作流)。", "子任务能在任务详情里**就地展开看过程**;打开子工作流的画布时,节点也会跟着亮起来了。"],
    en: ["Blockout reference frames **can finally draw your own models**. A product, prop or machine modelled in Blender and brought into Mosael used to be visible only in the 3D workbench — in every composition reference the automated pipeline handed to the image and video models, that object simply was not there, and neither side reported anything. Now it is drawn, using its own material colours; anything that genuinely cannot be drawn (Draco/meshopt-compressed mesh, over the triangle budget, missing file) is reported one by one with the reason.", "Imported 3D models now belong to the **workspace, not to one scene**: import a prop once and place it in any scene, from the new 模型库 group in the add menu. They used to hang off a scene — and a workflow creates a new scene on every run, so a Blender-built model could never reach automated production. Deleting a scene no longer deletes its models; deleting a model is refused while a scene still uses it, and the refusal names those scenes.", "New workflow node **Available 3D props**: it lists the workspace's models with their names and measured width/height/depth for the set designer, so an automatically built blockout can hold real props instead of boxes and cylinders. The dimensions are measured from the model, not typed in by you.", "When the agent looks at a model in Blender it is no longer limited to six fixed angles: it can ask for any azimuth and elevation, move in, frame only the part it just built, and switch to see-through shading to check whether two parts actually intersect — at whole-scene framing a seam is a few pixels wide, and that looks exactly like a part that was never built.", "**A failed turn no longer erases what that turn did.** A conversation that ran for three minutes across a dozen tool calls, cut off at the last step, used to leave nothing but \u201cthe agent failed\u201d \u2014 while those tool calls really happened. The record was never stored, and the UI dropped it a second time. Now the work is shown above the failure and the reason below it. This was the worst kind of asymmetry: the model's memory was restored, the record you can see was not.", "An answered choice card stays **a choice** in the transcript: it used to be flattened into a sentence and posted as if you had typed it, losing the question and the options. The UI now shows what was asked and which option you picked.", "**A running workflow can be stopped.** The only place to cancel used to be the × on a row in the task centre: open the detail dialog to watch the progress bar and there was no way out, and the workflow page's run button stayed a ▶ while the run was in flight. The toolbar now turns into **Stop** and the dialog carries **Cancel task**; cancelling is node-grained, so it stops after the current step finishes and everything already produced is kept.", "Five new official templates: four business-facing ones (highlight shorts, product on model, product pitch, fabric lookbook) and the most common enterprise shape of all — **montage from your own footage, with voice-over and subtitles**, for when you already have the material and only need it cut, voiced and captioned. Shot counts gained a cost gate so one run cannot quietly become dozens of shots.", "Structured output is no longer a gamble: a `json_object` node without a schema used to 400 every time, and when a model returns malformed JSON the error is now explained before it gets one chance to fix it. Model settings gained a **supports json_schema** switch, pre-answered for the providers we verified.", "Fixed the infinite canvas going completely unclickable — React Flow's stylesheet went missing along with the lazily loaded page. Plus three other regressions from lazy loading, and two places that displayed “we don't know yet” as if it were a conclusion (the agent's session list, a task's bound workflow).", "Sub-tasks now **expand in place** in the task detail dialog to show their progress, and opening a sub-workflow's canvas finally lights its nodes up as it runs."],
  },
  "v1.4.1": {
    zh: ["智能体能在你自己的 Blender 里建模:看场景结构、看视口画面、跑建模代码,再把结果收回 Mosael 的 3D 场景。它看得见自己改出来的东西 —— `view_scene` 和工具结果里的图片让它改完就能自己核对,而不是闭着眼睛一路改下去。", "代码执行分成两条路:沙箱里跑(算个数、处理一段文本)和**不隔离地在你电脑上跑**(`run_host_code`,装东西、动文件)。两条是**两档权限**,对前者放开不会连带放开后者 —— 自动放行的档位现在由每张确认卡自己声明,权限那一侧不再需要认识 Blender 是什么。", "工作流多了「搭建 3D 白模场景」「渲染白模参考」两个节点,智能体同样能用;白模渲染在后端跑,所以工作流可以拿一个 3D 场景当生成参考。官方模板「从主题到完整视频」升到 v6:三视图、场景设定图、3D 白模、首尾帧与运镜参考。", "生成节点能指定用哪一组素材,参考清单也能**整组**接进来,而不是一个个连线。节点之间「引用即依赖」:一个节点 `{{…}}` 引到谁,就等谁落定再开始。", "**付过钱的远端生成不再被本地超时掐掉**(ADR-0019):只有远端真的完成、或你自己取消,它才结束。循环里一项失败之后也不再开始新的付费迭代,而是把失败全部报出来。", "被时间线引用的素材现在可以直接删。引用它的片段**留在原位**变成「素材已删除」占位:位置、时长、变换、关键帧一样不动,时间线上是斜纹警示、监视器上是一块红屏,导出会明确拒绝而不是静默少一段。此前你得先在每条序列里把它们一个个找出来删掉。", "智能体也能删素材和项目了,走新的「永久删除」权限档:一次一批、卡上写清一共几个、都叫什么、连带影响多少个片段 —— 而这一档永远不会被自动放行。", "模型的上下文长度和最大输出不再靠猜:内置一张查证过的上限表(60+ 条,覆盖 OpenAI / Anthropic / DeepSeek / Gemini / Grok / Kimi / GLM / 通义 / MiniMax / 豆包 与常见开源权重),供应商目录不报上限时用它。模型设置里新增「最大输出 Token」,并分别显示「模型上限」和「不填时实际发多少」。", "修复「模型已用完本轮输出额度」:思考 token 和正文共用这份额度,而此前对 1M 窗口的模型只给 16,384 —— 一轮思考还没说完就被截断。", "打开一页快了很多:页面改成按需加载,主包从 4.20 MB 降到 0.28 MB;跑过的数据库迁移开始记账,启动不再每次从头跑一遍。", "修复计价规则的**读**没有过工作区的门 —— 别的工作区的计价规则能被读到。"],
    en: ["The agent can model inside your own Blender: read the scene graph, look at the viewport, run modelling code, and bring the result back into a Mosael 3D scene. It can see what it made — `view_scene` and images in tool results let it check its own work instead of editing blind.", "Code execution splits into two paths: sandboxed (compute something, process some text) and **unisolated, on your machine** (`run_host_code` — install things, touch files). They are **two separate permission tiers**, so opening up the first does not open up the second. Each confirmation card now declares which tier it belongs to, so the permission layer no longer needs to know what Blender is.", "Two new workflow nodes, “Build a 3D blockout” and “Render a blockout reference”, available to the agent as well; blockout rendering runs on the backend, so a workflow can use a 3D scene as a generation reference. The official “Topic to finished video” template moves to v6: orthographic views, a scene design sheet, a 3D blockout, first/last frames and camera-move references.", "Generation nodes can pick which asset group to use, and a reference list can be connected **as a whole** instead of one wire at a time. Between nodes, a reference is a dependency: whatever a node reads through `{{…}}` is waited for before it starts.", "**Paid remote generations are no longer cut short by a local timeout** (ADR-0019): they end when the remote side actually finishes, or when you cancel. A failed item in a loop no longer starts a new paid iteration either — every failure is reported instead.", "Assets used by a timeline can now be deleted directly. The clips that use them stay where they are and become “media offline” placeholders: position, duration, transform and keyframes untouched, hatched red on the timeline, a red frame in the monitor, and export refuses outright rather than silently dropping a shot. Previously you had to hunt them down in every sequence first.", "The agent can delete assets and projects too, on a new “permanent delete” permission tier: one batch at a time, with the card stating how many, which ones, and how many clips go offline — and this tier is never auto-approved.", "Model context and max-output limits are no longer guesses: a built-in table of verified limits (60+ entries across OpenAI, Anthropic, DeepSeek, Gemini, Grok, Kimi, GLM, Qwen, MiniMax, Doubao and common open weights) fills in whatever the provider catalog does not report. Model settings gain a “Max output tokens” field and show the model's ceiling and what is actually sent separately.", "Fixed “the model used up this turn's output budget”: reasoning tokens share that budget, and a 1M-context model was being given only 16,384 — enough to be cut off mid-thought.", "Pages open much faster: routes are loaded on demand, taking the main bundle from 4.20 MB to 0.28 MB, and database migrations that have already run are recorded so startup no longer replays them.", "Fixed pricing rules being **readable** across workspaces — the read path did not go through the workspace check."],
  },
  "v1.4.0": {
    zh: ["新增官方工作流「视频译配 · 字幕与配音」：逐句转写、逐句翻译、按**原时间码**铺译文字幕，再逐条配音并变速压回原段落长度；原声保留在自己的轨上，只是被压低。", "字幕配音也能在对话里让智能体做，确认卡会写清这一步影响几条字幕、会不会压回原长度、原声会怎样 —— 它是花钱的动作。", "配音之后原声怎么办现在是可选的四档：配音说话时压低、静音（译配用它）、保持原样、只去掉人声留背景音；剪辑台、智能体和工作流三处都能选，都撤得回来。", "一次任务只响一次：一次译配此前会弹十几条「完成」（每句配音、顺手排的预览代理各一条），现在它们归到这次任务名下，任务中心只报一次；预览代理只在失败时说话。", "新增降噪：内置引擎先量噪声再下手，去掉底噪不动音乐；以说话为主的录音可选开源的 DeepFilterNet（效果最好，设置里下载一次）或 RNNoise（随应用带着）。素材库、剪辑台、工作流节点和智能体都能用，产出新素材、原片不动。", "新增人声与背景音分离：素材库、剪辑台、工作流节点和智能体共用一个能力，产出两份新素材，原片不动；译配模板靠它只静音人声、保住背景音乐。", "设置页按用途重新分组；AI Studio 新增「音频」页做语音和播客；给字幕配音搬进剪辑台的「配音」页，范围跟着时间线选中走。", "3D 场景卡片有了缩略图：从场景数据直出的俯视平面，场景一改缩略图跟着变，不是一张会和数据脱节的截图。", "目录认不出的模型，参数可以自己写下来：手填的别名、经中转配的同一个模型，此前在生成界面上一个参数都没有；现在按能力分别指一份参数组，而且认不出时界面会说出这条通道发得出哪几项，不再沉默。", "修复配音压不住原声 —— 成片里两个人同时说话。原片在视频轨上时，闪避从来没生效过（实测 −0.03 dB，修后 −10.35 dB）。"],
    en: ["New official workflow \u201cTranslated dubbing with subtitles\u201d: transcribe and translate line by line, lay subtitles on the **original** timecodes, then dub each line and time-compress it back into its own slot. The original audio stays on its track, only ducked.", "The agent can dub subtitles from a conversation too. The confirmation card states how many subtitles are affected, whether the dub is compressed back to length, and what happens to the original audio \u2014 it is a paid action.", "What happens to the original audio after dubbing is now a choice of four: lower it while the dub speaks, mute it (for translated dubbing), leave it as is, or remove only the voice and keep the music. The editor, the agent and the workflow node all offer it, and all of it can be undone.", "One run, one notification: a translated dub used to raise a dozen \u201cdone\u201d toasts (one per spoken line, one for the proxy queued afterwards). Those now belong to the run that started them, the task centre announces it once, and proxy transcoding only speaks up when it fails.", "New noise reduction: the built-in engine measures the noise before it acts and removes background hiss without touching music; for speech-led recordings, pick the open-source DeepFilterNet (best results, one download in Settings) or RNNoise (ships with the app). Available from the media library, the editor, a workflow node and the agent, always as a new asset.", "New voice / background separation: the media library, editor, workflow node and agent share one capability that produces two new assets and leaves the source untouched; the dubbing template uses it to mute only the voice and keep the music.", "Settings are regrouped by purpose; AI Studio gains an Audio tab for speech and podcasts; subtitle dubbing moves to the editor\u2019s Voice tab and follows the timeline selection.", "3D scene cards have thumbnails: a top-down plan drawn from the scene data itself, so it follows edits instead of drifting from a stale screenshot.", "Models the built-in catalog does not recognise can now carry parameters you write yourself: hand-typed aliases and the same model behind a relay used to show no parameters at all. Point each capability at a parameter set \u2014 and when a model is unrecognised the screen now says which fields that channel can send instead of staying silent.", "Fixed dubbing failing to duck the original \u2014 two people talking at once in the export. When the source sits on a video track, ducking never applied (measured \u22120.03 dB; \u221210.35 dB after the fix)."],
  },
  "v1.3.1": {
    zh: ["智能体对话框支持 `@` 引用素材、笔记、画板与工作流：正文写名字，id 走结构化字段；发出去仍是可点开的胶囊。", "思考档位按供应商查表：关不掉的模型不再给「关闭」，发不出档位的连接说得出原因。", "修复深色下菜单分组线与页面同色而看不见 —— 菜单、命令面板、通知与任务面板一并恢复。", "生成页「引擎参数」重排为引擎／出片规格／输入素材／调参四块，标题行不再随滚动移出视野。"],
    en: ["The agent composer supports `@` references to assets, notes, boards and workflows: names in the text, ids in structured fields, and clickable chips after sending.", "Thinking levels now come from a per-vendor table: models that cannot stop thinking no longer offer \"Off\", and connections that cannot carry a level say why.", "Fixed menu separators being the same colour as the surface in dark mode — menus, the command palette and the notification and task panels all get their dividers back.", "The generation engine-settings panel is grouped into engine, output, inputs and tuning, with a header that no longer scrolls away."],
  },
  "v1.3.0": {
    zh: ["选项一多的下拉自动带搜索：模型、音色、字体、LUT、发布平台与工作流的上游输出。", "团队讨论改成右侧侧栏，画板与工作流共用；工作流页新增讨论中心。", "「在画布中查看」与加标记会避开右栏面板，不再跳到停靠的智能体底下。", "智能体输入框里的附件与笔记引用收成一排，带缩略图，点开即可预览。"],
    en: ["Long dropdowns now search: models, voices, fonts, LUTs, publishing platforms and upstream workflow outputs.", "Team discussions moved to a right-side sheet, shared by boards and workflows; workflows gained the discussion centre.", "Jumping to a comment or dropping a marker now avoids docked and floating side panels.", "Composer attachments and note references share one row, with thumbnails and click-to-preview."],
  },
  "v1.0.0": {
    zh: ["1.0 正式发布，提供 macOS Apple Silicon 与 Windows x64 安装包。", "统一磨砂界面、剪辑与媒体预览，内置中英文及手写字体。", "整合智能体语音、工作流与定时任务、插件连接和多执行器发布。", "更新 40 篇中英文指南与全部当前界面配图、GIF、录屏，README 加入错层截图展示。"],
    en: ["The stable 1.0 release is available for macOS Apple Silicon and Windows x64.", "Unified frosted interface, rebuilt editing and media previews, and bundled multilingual and handwritten fonts.", "Includes agent voice, workflows and scheduling, plugin connections and multi-worker publishing.", "Refreshed 40 bilingual guides and all current captures, GIFs and recordings, with a layered README showcase."],
  },
  "v1.0.0-beta5": {
    zh: ["统一半透明模糊弹窗与菜单，柔化边界，适配自定义背景。", "重构剪辑工作区与音视频预览，修复长标题、画布菜单定位和可视区域居中。", "新增全局中英文字体，包含 Caveat 与 Kalam 手写风格。", "全面更新中英文文档与明暗主题实拍，新增外观、定时任务指南和可暂停录屏。"],
    en: ["Unified frosted dialogs and menus, softened boundaries and preserved custom backgrounds.", "Rebuilt the editor and media previews; fixed long titles, canvas menus and visible-area centering.", "Added global Chinese/English font combinations, including Caveat and Kalam handwriting.", "Refreshed bilingual guides and light/dark captures, with Appearance and Scheduled Tasks guides and controllable recordings."],
  },
  "v1.0.0-beta4": {
    zh: ["修复百度网盘导入、上传时缺少工作区的问题，完善插件授权与参数说明。", "新增听写、语音回复和可打断的免提对话。", "修复任务取消、嵌套工作流、执行器失联，以及剪辑预览与导出不一致的问题。", "代码节点改用 Docker 隔离；自定义外部执行器需要升级租约协议。"],
    en: ["Fixed missing workspace context in Baidu Netdisk imports and uploads, plus plugin authorization and parameter descriptions.", "Added dictation, spoken replies and interruptible hands-free conversations.", "Fixed task cancellation, nested workflows, lost workers and differences between editor preview and export.", "Code nodes now require Docker isolation. Custom external workers must adopt the lease protocol."],
  },
  "v1.0.0-beta2": {
    zh: ["新增屏幕与摄像头同步录制、系统音频采集和悬浮录制控制。", "新增片段遮罩、投影、数据备份与诊断。", "加入工作流社区、完整视频生成与口播整理模板，以及逐节点运行历史。"],
    en: ["Added synchronized screen and camera recording, system audio and a floating recording controller.", "Added clip masks, shadows, data backup and diagnostics.", "Introduced workflow templates for video generation and transcript cleanup, plus per-node execution history."],
  },
  "v1.0.0-beta1": {
    zh: ["统一旧数据升级策略，改进画板自动保存与冲突处理。", "扩展发布检查，覆盖浏览器扩展和中英文官网。", "预发版独立标记，不替换最新正式版。"],
    en: ["Unified data upgrade handling and improved canvas autosave and conflict recovery.", "Extended release checks to cover the browser extension and bilingual website.", "Marked beta releases separately without replacing the latest stable release."],
  },
};
