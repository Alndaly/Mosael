# 1.5.1 发布验证

发布提交 `9308b8b69a6f6944b1829c02989b9da80d5d7e65`(tag `v1.5.1` 指向它,本地打完再推)。
Release:<https://github.com/Alndaly/Mosael/releases/tag/v1.5.1>,2026-09-24T01:08:20Z 转正式、标为 latest。

## 构建

云端签名 + 本机钥匙串公证交接(`notarization=local`)。

- Run [35908731067](https://github.com/Alndaly/Mosael/actions/runs/35908731067),`headSha` 已核对为 `9308b8b6`。
- 四个 job 全绿:`signing`、`test / test`、`build (macos-latest, release/*.dmg)`、
  `build (windows-latest, release/*.exe)`。`draft` / `publish` 按设计跳过。
- 第一次 `gh run download` 在 mac 包写到一半时断开(`unexpected EOF`),按 artifact 分别重下,大小与
  解压、签名校验都正常。

## 测试闸(云端 run 35908731067 的 `test / test`,发布提交上)

| 套件 | 结果 |
| --- | --- |
| 仓库根 `pnpm lint`(oxlint + ruff) | All checks passed |
| backend `pytest` | 4053 passed, 5 skipped |
| frontend `vitest` | 326 files / 1935 tests passed |
| agent-sidecar 测试 | 全部 pass / 0 fail |
| browser-extension `vitest` | 14 files / 71 tests passed |
| website `node --test` | 17 pass / 0 fail(含版本一致性) |

本机 backend 全量里有 27 条依赖 `run_code` 沙箱(`python:3.14-alpine` 镜像)的测试失败,原因是这台
机器没有这个镜像;云端 runner 有,它们在上面的 4053 条里跑过。

## macOS 签名与公证

| 步骤 | 结果 |
| --- | --- |
| `Info.plist` 版本 | `1.5.1` |
| `verify-mac-signing.cjs --signature-only` | Verified signed artifact pending notarization with WebAuthn keychain group `27W3D2ZUT9.dev.mosael.app.webauthn` |
| app zip 公证 | `2ec8ed8e-4445-406f-bc6a-12c86e5919ad` — **Accepted** |
| `stapler staple` + 全量 verify | `accepted` / `source=Notarized Developer ID` |
| `test/bundle.smoke.mjs` | `bundle smoke passed (darwin, app 1.5.1)`;`WebAuthn configured=true, device available=true` |
| DMG 签名 + 公证 | `6a77e6f6-875b-4e3d-89f0-448f1ac659fc` — **Accepted**;staple / validate 通过,`spctl` accepted |
| 只读挂载 DMG | 内部应用 `1.5.1`,`codesign --verify --deep --strict` 通过,票据 validate 通过,`spctl --type execute` accepted,带 `/Applications` 链接 |

第一次公证在终端面板里报 `No Keychain password item found for profile: mosael-release`:当时电脑
处于锁屏,钥匙串里的公证凭据读不到。解锁后同一个 profile 正常,没有改动任何钥匙串或系统设置。

## 附件(`dist/1.5.1/`,13 个 + SHA256SUMS)

浏览器扩展与 10 个插件包从发布提交打出;插件包逐个用应用自己的安装器
(`registry.install_archive`)装进临时目录并解析清单,全部通过。上传后逐个比对 GitHub API 返回的
digest、大小与本地文件:**13 个全部一致**。

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `Mosael-1.5.1-arm64.dmg` | 320,260,842 | `01760693f548334f…` |
| `Mosael.Setup.1.5.1.exe` | 247,955,322 | `06b474d2f9c4fd65…` |
| `dev.mosael.aliyun-oss.zip` | 15,197 | `d513a6919814f644…` |
| `dev.mosael.aws-s3.zip` | 15,169 | `dbb942237dc799b0…` |
| `dev.mosael.baidu-pan.zip` | 11,731 | `1f684d2e71e2cca6…` |
| `dev.mosael.blender.zip` | 11,557 | `8254f742de002975…` |
| `dev.mosael.mcp-everything.zip` | 2,095 | `beb6ad12ed510e40…` |
| `dev.mosael.remotion.zip` | 23,811 | `0c7e750c90c8d0d9…` |
| `dev.mosael.tencent-cos.zip` | 13,559 | `6d5034f14be930ac…` |
| `dev.mosael.text-toolkit.zip` | 2,443 | `e759f7da40675d57…` |
| `dev.mosael.tikhub.zip` | 3,288 | `fd5e6e979c8ca060…` |
| `dev.mosael.volcengine-tos.zip` | 15,226 | `2c35a19591ef54ca…` |
| `mosael-browser-extension.zip` | 4,611,396 | `e6d375c6c15fdcd0…` |

国内下载:需要把 `dist/1.5.1/` 整个上传到百度网盘分享文件夹「Mosael」下(官网链接不用改)。
