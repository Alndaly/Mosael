# 1.6.0 发布验证

发布提交 `8eaa198a3a952a5932cbc926aeac5fb7851d52d2`(tag `v1.6.0` 指向它,本地打完再推)。
Release:<https://github.com/Alndaly/Mosael/releases/tag/v1.6.0>,2026-09-26T10:41:43Z 转正式、标为 latest。
插件市场索引 `releases/latest/download/registry.json` 发布后返回 200(1.6.0 起应用读的就是它)。

## 构建前的三次重来

- 第一次发版构建(run 36224332418)在两个平台的打包冒烟都失败:`test/upgrade_db_fixture.py` 还期望旧的
  画板表单形状,而 ADR 0021 的迁移会写上 `form.producer`。夹具跟上,另加后端测试让 CI 也跑「播种旧库 →
  迁移 → 校验」(`e2fe8884`),以后不必等到发版才发现。
- 之后两次构建按用户要求中途取消,把统计页分 tab、插件与画板的几处整改一并收进这一版。
- 最后一轮把画板和工作流分开(`8eaa198a`,ADR 0021 修订):画板只放内容变换,流程 / 数据步骤回到工作流。

## 构建

云端签名 + 本机钥匙串公证交接(`notarization=local`)。

- Run [36233131445](https://github.com/Alndaly/Mosael/actions/runs/36233131445),`headSha` 已核对为 `8eaa198a`;
  同一提交推送触发的 CI(run 36233124088)通过。
- 四个 job 全绿:`signing`、`test / test`、`build (macos-latest, release/*.dmg)`、
  `build (windows-latest, release/*.exe)`。`draft` / `publish` 按设计跳过。
- 推 tag 触发的 run 36236416878 因缺云端公证凭据整条跳过(预期),没有覆盖附件。

## 测试闸(云端 run 36233131445 的 `test / test`,发布提交上)

| 套件 | 结果 |
| --- | --- |
| 仓库根 `pnpm lint`(oxlint + ruff) | All checks passed |
| backend `pytest` | 5134 passed, 7 skipped(含 Docker 沙箱那批) |
| frontend `vitest` | 413 files / 2486 tests passed |
| agent-sidecar、browser-extension、website 各组 `node --test` | 全部 pass / 0 fail |

本机 backend 全量里依赖 `run_code` 沙箱(`python:3.14-alpine` 镜像)的 27 条失败,原因是这台机器没拉
这个镜像;云端 runner 有,它们在上面的 5134 条里跑过。

## macOS 签名与公证

| 步骤 | 结果 |
| --- | --- |
| `Info.plist` 版本 | `1.6.0` |
| `verify-mac-signing.cjs --signature-only` | Verified signed artifact pending notarization with WebAuthn keychain group `27W3D2ZUT9.dev.mosael.app.webauthn` |
| app zip 公证 | `ba66f342-6ff6-419f-b94a-79a98014345b` — **Accepted** |
| `stapler staple` + 全量 verify | `accepted` / `source=Notarized Developer ID` |
| `test/bundle.smoke.mjs` | `bundle smoke passed (darwin, app 1.6.0)`(含旧库升级校验);`WebAuthn configured=true, device available=true` |
| DMG 签名 + 公证 | `0f1fbb8e-ef59-4626-99d7-b15821503d58` — **Accepted**;staple / validate 通过,`spctl` accepted |
| 只读挂载 DMG | 内部应用 `1.6.0`,签名与票据校验通过,`source=Notarized Developer ID` |

`notarytool` 在关掉沙箱的 shell 里报「No Keychain password item found for profile: mosael-release」,
凭据其实在:默认沙箱里同一条命令正常。公证命令一律在默认沙箱里跑。

应用包里只找到两个公开的 CA 根证书包(`certifi/cacert.pem`),没有 `.env`、私钥或证书文件。

## 附件(`dist/1.6.0/`,10 个 + SHA256SUMS)

浏览器扩展与 6 个插件包从发布提交打出;插件包逐个用应用自己的安装器(`registry.install_archive`)装进
临时目录并解析清单,全部通过,包里没有缓存、`.env` 或密钥。`registry.json` 由
`scripts/sync-plugin-registry.py --release v1.6.0` 生成,下载地址钉在 `v1.6.0`;随应用发的 ComfyUI(1.4.0)
与对象存储(1.0.0)只登记、不出包。上传后逐个比对 GitHub API 返回的 digest、大小与本地文件:
**10 个全部一致**。

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `Mosael-1.6.0-arm64.dmg` | 327,256,547 | `0ee26728d67d533d…` |
| `Mosael.Setup.1.6.0.exe` | 248,561,052 | `aa5359c3f19ca785…` |
| `dev.mosael.baidu-pan.zip` | 15,473 | `5daeabc3eddfaf1a…` |
| `dev.mosael.blender.zip` | 11,896 | `cd94cca22026d4b3…` |
| `dev.mosael.manim.zip` | 63,296 | `cc70018d980759a5…` |
| `dev.mosael.remotion.zip` | 33,568 | `4e62235b0cb20391…` |
| `dev.mosael.text-toolkit.zip` | 4,294 | `00966ba8f34efdb3…` |
| `dev.mosael.tikhub.zip` | 3,537 | `3e67c864ad19dae3…` |
| `mosael-browser-extension.zip` | 4,615,866 | `baf9963251fa5cad…` |
| `registry.json` | 28,702 | `bf5e4b453171cea1…` |

国内下载:`dist/1.6.0/` 打成 `1.6.0.zip`(`ditto --norsrc --noextattr --noqtn`,解压后按 SHA256SUMS
校验 10 个全部 OK)上传到百度网盘分享文件夹「Mosael」下(官网链接不用改)。
