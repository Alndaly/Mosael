# 1.5.2 发布验证

发布提交 `bb4ded2de2e94385f6db417e5a93ab1842129908`(tag `v1.5.2` 指向它,本地打完再推)。
Release:<https://github.com/Alndaly/Mosael/releases/tag/v1.5.2>,2026-09-24T16:16:53Z 转正式、标为 latest。

## 先把 CI 修绿

这一版中途 CI 时红时绿:同一个提交 `32f4d769`,推送触发的 CI 红、发版流水线的测试 job 绿,红的
地方还各不相同(`test_agent_host` 读到 running、迁移覆盖测试撞 `duplicate column`)。两个都是真竞态,
在 `4e6dd51a` 按根因修掉(队列 drain 空抢会话;测试夹具等后台线程只等 5 秒、等不完照样重建库),
没有靠重跑。发布提交先等推送触发的 CI(run 36010191570)通过,再起发版构建。

## 构建

云端签名 + 本机钥匙串公证交接(`notarization=local`)。

- Run [36013936243](https://github.com/Alndaly/Mosael/actions/runs/36013936243),`headSha` 已核对为 `bb4ded2d`。
- 四个 job 全绿:`signing`、`test / test`、`build (macos-latest, release/*.dmg)`、
  `build (windows-latest, release/*.exe)`。`draft` / `publish` 按设计跳过。

## 测试闸(云端 run 36013936243 的 `test / test`,发布提交上)

| 套件 | 结果 |
| --- | --- |
| 仓库根 `pnpm lint`(oxlint + ruff) | All checks passed |
| backend `pytest` | 4227 passed, 7 skipped(含 Docker 沙箱那批) |
| frontend `vitest` | 341 files / 2045 tests passed |
| agent-sidecar、browser-extension、website 各组 `node --test` | 全部 pass / 0 fail |

本机 backend 全量里依赖 `run_code` 沙箱(`python:3.14-alpine` 镜像)的测试失败,原因是这台机器没拉
这个镜像;云端 runner 有,它们在上面的 4227 条里跑过。

## macOS 签名与公证

| 步骤 | 结果 |
| --- | --- |
| `Info.plist` 版本 | `1.5.2` |
| `verify-mac-signing.cjs --signature-only` | Verified signed artifact pending notarization with WebAuthn keychain group `27W3D2ZUT9.dev.mosael.app.webauthn` |
| app zip 公证 | `ddc07b73-ab6b-4384-a574-66b1274bcbf6` — **Accepted** |
| `stapler staple` + 全量 verify | `accepted` / `source=Notarized Developer ID` |
| `test/bundle.smoke.mjs` | `bundle smoke passed (darwin, app 1.5.2)`;`WebAuthn configured=true, device available=true` |
| DMG 签名 + 公证 | `1ce95ce0-aa09-4a0d-8d44-cd6ed3160cdf` — **Accepted**;staple / validate 通过,`spctl` accepted |
| 只读挂载 DMG | 内部应用 `1.5.2`,签名与票据校验通过,`source=Notarized Developer ID` |

DMG 第一次提交时 `notarytool` 在上传途中崩溃(`Bus error: 10`),留下一条永远停在 In Progress 的
提交 `ce000b29-1069-41a0-af8d-84841d29fd87`(文件没传完整;1.5.1 的 DMG 三分钟就过)。等了 43 分钟后
确认它不会有结果,按流程先查过历史、再对同一个已签名的 DMG 重新提交,新提交几分钟内 Accepted。

## 附件(`dist/1.5.2/`,13 个 + SHA256SUMS)

浏览器扩展与 10 个插件包从发布提交打出;插件包逐个用应用自己的安装器
(`registry.install_archive`)装进临时目录并解析清单,全部通过。上传后逐个比对 GitHub API 返回的
digest、大小与本地文件:**13 个全部一致**。

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `Mosael-1.5.2-arm64.dmg` | 327,072,029 | `9b1350f078071148…` |
| `Mosael.Setup.1.5.2.exe` | 248,056,546 | `b0c6f83191a4f80f…` |
| `dev.mosael.aliyun-oss.zip` | 15,197 | `13e3f1130f29f6e5…` |
| `dev.mosael.aws-s3.zip` | 15,169 | `297a2348df36b401…` |
| `dev.mosael.baidu-pan.zip` | 11,690 | `5e6b8bb18b6b53b2…` |
| `dev.mosael.blender.zip` | 11,557 | `af3133edd954a7a3…` |
| `dev.mosael.mcp-everything.zip` | 2,095 | `c76bc621feea5c46…` |
| `dev.mosael.remotion.zip` | 23,805 | `627dcae2626f6c33…` |
| `dev.mosael.tencent-cos.zip` | 13,559 | `b0375e9e6596c3f7…` |
| `dev.mosael.text-toolkit.zip` | 2,443 | `de001cd7b1cd480e…` |
| `dev.mosael.tikhub.zip` | 3,288 | `d2656330daf72c91…` |
| `dev.mosael.volcengine-tos.zip` | 15,226 | `45a3517c2ac1a6e0…` |
| `mosael-browser-extension.zip` | 4,615,866 | `06494d31f7be1984…` |

国内下载:`dist/1.5.2/` 整个上传到百度网盘分享文件夹「Mosael」下(官网链接不用改)。
