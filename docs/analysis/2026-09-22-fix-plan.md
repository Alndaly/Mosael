# 1.4.3 修复清单:55 条,逐条对账

> 来源:[五路审计综合](2026-09-22-synthesis.md) 汇总的 55 条问题。
> 范围:**全部修完才发 1.4.3**(2026-09-22 确认)。
> 状态标记:`✅` 已修并推送 · `🔧` 进行中 · `⬜` 待办。

## 已拍板的四处产品决定

这四条的修法不是技术问题,记在这里免得后面再议一遍:

| 议题 | 决定 |
| --- | --- |
| 首页 `WorkspaceSummaryOut` 八个没人读的字段 | **用起来**:费用磁贴改显示金额(不是调用次数),费用图下面加一行按供应商的分布 |
| 删除一个还被引用的东西 | **拒绝 + 点名谁在用**,和现有 `delete_model` 一致;数据里不允许出现坏引用 |
| 两个聊天面板的流式协议分岔 | **抽出共用协议层**(怎么读流、怎么拼时间线、怎么处理错误),两个面板各自保留 UI |
| `record_usage` 的兜底幂等键 | **做成真幂等**:`operation + source_type + source_id + 调用方给的轮次号`,不编时间戳 |

---

## 一、今天就在伤人(5)

| | 问题 | 出处 |
| --- | --- | --- |
| ✅ | `finish()` 先关看门狗再无限期等 —— 会话永久卡「思考中」 | 进程 2.4 · `d2fe7aa0` |
| ✅ | 子智能体收不到中止信号(同一条链的 sidecar 端) | 进程 2.4 · `78697a8d` |
| ✅ | sidecar 说了「这条没接住」,后端从不读 —— 消息被静默吞掉 | 进程 2.1 |
| ✅ | 凭据租约:后端分两种 409,sidecar 当成同一种 → 下一轮被登出 | 进程 2.2 · `e0444d38` |
| ✅ | 关掉代理生成后预览永远停在「转码中」,还每 2 秒问一次 | 前端 2.7 |
| ✅ | 模型上限在直连那八个调用点上发不出去(思考档位见 AI 2.4)| AI 2.1 |

## 二、结构性高危(9)

| | 问题 | 出处 |
| --- | --- | --- |
| ✅ | 工作流引擎抱着数据库连接等嵌套的活儿,并行度没按连接池预算 | 后端 2.1 |
| ✅ | 子工作流输出契约的 `or` 回退,把「空」和「没有」混成一件事 | 后端 2.2 |
| ✅ | AI 编排是工作流这条链的最后一环,三处都断 | 后端 2.3 |
| ⬜ | 浏览器执行器是第三条 claim/report 通道,ADR-0002 一条没落 | 进程 2.3 |
| ⬜ | 两个聊天面板各实现一遍流式协议,已分岔 → **抽共用协议层** | 前端 2.3 |
| ⬜ | `linked_clip_id` 四层都有,没有任何一处写它 | 前端 2.1 |
| ⬜ | 首页八个没人读的字段 → **用起来** | 前端 2.2 |
| ✅ | 降级是静默的:三处触发点,零处记录 | AI 2.2 |
| ✅ | 放行判断每次真花钱,账上一条都没有 | AI 2.3 |

## 三、棘轮层(11,已修 8)

| | 问题 | 出处 |
| --- | --- | --- |
| ✅ | 升级快照实际关了 17 个版本 | 工程 P0-1 · `d2d7e16a` |
| ✅ | 执行器输出棘轮在该响时沉默 | 工程 P0-2 · `770be6c3` |
| ✅ | 数据归属豁免整个 `app/api/routes/`(19 处直接建行)→ 改成 ALLOWLIST | 工程 P1-1 |
| ⬜ | 「接口字段 → 前端消费点」这一跳没有任何检查,而且已经在漏 | 工程 P1-3 |
| ✅ | 进程级全局状态测试间串台,吃掉 13% 套件时间 | 工程 P1-2 |
| ⬜ | 46/69 个迁移只跑过「无事可做」那条分支 | 工程 P2-1 |
| ✅ | 记账表没有内容指纹 → 指纹放**仓库里**当棘轮(按指纹重跑不安全,见测试说明) | 工程 P2-2 |
| ✅ | `RELEASING.md` 说逐项核对,实际 5 处只对了 2 处 → 改成**扫整棵文档树** | 工程 P2-3 |
| ✅ | `test_docs_do_not_point_at_ghosts` 正则太窄 → 顶层目录问 git 要,后缀不再枚举 | 工程 P2-4 |
| ✅ | `schema_baseline.json` 少 17 张表 → 补齐,并改成**照基线建库跑真迁移**(抓到一条启动崩) | 工程 1.3 |
| ⬜ | `unlayeredGlobals` 只读 `tokens.css`;`typeScale` 只扫 `.ts/.tsx`;`featureBoundaries` 只拦二元环 | 前端 2.8/2.13、工程 1.10 |
| ⬜ | 文档里手写的数字全部已经错了;棘轮的「理由」没有同步机制 | 工程 P3-1 / P3-2 |

## 四、后端领域 ✅ 全清

| | 问题 |
| --- | --- |
| ✅ | 任务总线自己一句翻不了的中文,棘轮扫不到 `say(job, "中文")`(2.4) |
| ✅ | 确认卡措辞写入时冻结中文,和 `Job.message` 的教训相反(2.5) |
| ✅ | 删除被引用对象三种答案 → **统一成拒绝 + 点名**(2.6) |
| ✅ | `NODE_TYPES.label` 是 i18n key,两处当人话落库(2.7) |
| ✅ | 两处残留兼容分支,按仓库规矩该是迁移或删除(2.8) |
| ✅ | 时间线算子清单自称全集,实为 28 取 10,两份手写没比对(2.9) |
| ✅ | `questions.answer` 的 docstring 声称了一道它不做的校验(2.10) |

## 五、前端(10 条剩余)

| | 问题 |
| --- | --- |
| ⬜ | 自动放行留了痕,而人看不到(2.4) |
| ⬜ | `api/domains/*` 14 个手写接口是生成类型的影子(2.5) |
| ⬜ | 「旗子在最上面」两块画布两个数,而两个数不一样(2.6) |
| ⬜ | `--scene-row-bleed` 收敛了一次,又被三个字面量兜回去(2.9) |
| ⬜ | `selectedClipId` 与 `selectedClipIds` 是同一件事的两份(2.10) |
| ⬜ | `api/client.ts` 这个桶少了两块,校验它的测试是手写清单(2.11) |
| ⬜ | `contracts/shared-constants.json` 的校验只跑在后端(2.12) |

## 六、进程边界(4 条剩余)

| | 问题 |
| --- | --- |
| 🔧 | 跨进程的时间预算一条都不在契约里(2.5)—— 凭据那两条已进契约,其余待办 |
| ⬜ | Blender 借用插件那条 60 秒通用预算(2.6) |
| ✅ | `X-Mosael-Client` 在两个客户端上是两个意思(2.7)→ 定语法,拆两列 |
| ✅ | 扩展写死 `Accept-Language: zh-CN`(2.8)→ 跟浏览器 UI 语言 |
| ⬜ | 重启之后没人收尾的两处状态(2.9) |
| ⬜ | 协议上的死字段:一侧写没人读、一侧声明没人发(2.10) |
| ✅ | 官网的版本耦合:自动校验覆盖两项,漏三项(2.11)—— 与工程 P2-3 同一条 |
| ✅ | 开发态不构建 sidecar,也不 watch system(2.12)→ 接进 dev + 棘轮 |
| ✅ | CORS 名单写死 8800,而端口是可配的(2.13) |
| ✅ | 扩展的消息分发靠兜底分支(2.14)→ 穷尽 switch + never,编译器接管 |
| ⬜ | sidecar 没有 `getppid()` 看门狗(进程 2.4 顺带) |

## 七、AI 供应商 ✅ 全清

| | 问题 |
| --- | --- |
| ✅ | 「发不出思考档位」被当成「这个模型不思考」(2.4) |
| ✅ | 本地/无鉴权端点拿不到模型目录,同一条规矩只修在一处(2.5) |
| ✅ | 兜底幂等键编进时间戳 → **做成真幂等**(2.6) |
| ✅ | worker 写管道失败后可能永远留在池子里且永远 busy(2.7) |
