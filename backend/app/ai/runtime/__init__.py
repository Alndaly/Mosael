"""**本地**跑的模型:下载、常驻、执行。

和同级的 `providers/` 是一件事的两面 —— 那边是"把活派给别人家的 API",这边是"在这台机器上
自己跑"。此前这两半分处 `ai/providers/` 与 `audio/`,分界线是「输出什么介质」而不是「谁在跑」。

这一层**只依赖 core**:没有数据库、没有领域概念。搬进来之前它就是这个样子,所以搬迁没有阻力
—— 这本身也是它一直属于这里的证据。
"""
