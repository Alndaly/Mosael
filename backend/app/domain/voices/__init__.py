"""音色库、配音、字幕配音、播客 —— 声音这一侧的**领域流程**。

和 `domain/generation/` 是同一类东西:建任务、写素材、记账。它此前住在 `audio/` 里,
和本地模型运行时、远程适配器混在一起,而那三者分属三层。

引擎目录(engine_catalog)也在这里:它回答的是「这个部署现在能用什么」—— 要读本地模型的
就绪状态和用户配的模型,那是领域的事,不是适配器的事。
"""
